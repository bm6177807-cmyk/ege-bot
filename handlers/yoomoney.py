# handlers/yoomoney.py
# YooMoney callback/payment handlers live in handlers/profile.py.
# This module only exposes an empty router so imports continue to work.
from aiogram import Router

router = Router()